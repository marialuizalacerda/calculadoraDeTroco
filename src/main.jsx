import React from 'react';
import ReactDOM from 'react-dom';
import ChangeCalculator from '/src/App.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <ChangeCalculator />
  </React.StrictMode>
)
